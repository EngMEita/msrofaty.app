<x-app-layout><x-slot name="header"><h2>إدارة العملات</h2></x-slot><div class="py-8"><div class="max-w-5xl mx-auto px-4">@if(session('message'))<div class="alert mb-4">{{ session('message') }}</div>@endif<div class="bg-white rounded-lg p-6 shadow-sm mb-5"><h3 class="font-bold mb-4">إضافة عملة</h3><form method="POST" action="{{ route('acp.currency.store') }}" class="grid grid-cols-1 md:grid-cols-4 gap-3">@csrf<input name="code" maxlength="3" placeholder="SAR" required><input name="name" placeholder="ريال سعودي" required><input name="symbol" placeholder="ر.س"><label class="flex items-center gap-2"><input type="checkbox" name="is_base" value="1"> العملة الأساسية</label><button class="btn-ms md:col-span-4" type="submit">حفظ</button></form></div><div class="bg-white rounded-lg shadow-sm overflow-x-auto"><table><thead><tr><th>الكود</th><th>الاسم</th><th>الرمز</th><th>الحالة</th><th>إجراء</th></tr></thead><tbody>@forelse($currencies as $currency)<tr><td>{{ $currency->code }}</td><td>{{ $currency->name }}</td><td>{{ $currency->symbol }}</td><td>{{ $currency->is_base ? 'أساسية' : ($currency->active ? 'نشطة' : 'متوقفة') }}</td><td><form method="POST" action="{{ route('acp.currency.destroy',$currency) }}" onsubmit="return confirm('حذف العملة؟')">@csrf @method('DELETE')<button class="text-red-600" type="submit">حذف</button></form></td></tr>@empty<tr><td colspan="5" class="text-center py-6">لا توجد عملات بعد.</td></tr>@endforelse</tbody></table></div></div></div></x-app-layout>
